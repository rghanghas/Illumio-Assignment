The assignment was pretty straightforward. All the variables, functions and file names are self explanatory.

To compile and run:

javac Illumio.java
java Illumio


Assumptions:

- Program only supports default log format, not custom and the only version that is supported is 2.
- Save the input documents (flowLogs.txt and lookup.csv) in the same folder.

